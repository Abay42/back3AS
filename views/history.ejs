<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Search History</title>
  <link rel="stylesheet" type="text/css" href="/style.css" />
</head>
<body>
  <div class="container">
    <h1>Search History</h1>
    <% if (history && history.length > 0) { %>
      <ul>
        <% history.forEach(search => { %>
          <li>
            <p><strong>City was searched:</strong> <%= search.city %></p>
            <p><strong>Timestamp:</strong> <%= new Date(search.timestamp).toLocaleString() %></p>
            <p><strong>Temperature:</strong> <%= search.weatherData.main.temp %> °C</p>
            <p><strong>Description:</strong> <%= search.weatherData.weather[0].description %></p>
          </li>
        <% }); %>
      </ul>
    <% } else { %>
      <p>No search history available.</p>
    <% } %>
    <a href="/">Back to Home</a>
  </div>
</body>
</html>
